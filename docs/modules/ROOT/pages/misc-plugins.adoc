= Miscellaneous Plugins
:description: Aggregation, version platforms, starters, root projects, and top-level bundles.
:toc: left
:toclevels: 3

The `plugins-misc` module provides utility plugins for project structure roles (root, starter, platform) and top-level bundles that compose plugins from all other modules.

== Aggregation Plugin

Aggregates reports (JaCoCo, test reports, SBOM, plugin analysis) from subprojects into the root project.

Plugin ID:: `dev.daymor.ultimanexus.jvm.gradle.feature.aggregation`

[source,kotlin]
----
plugins {
    alias(libs.plugins.ultimanexus.jvm.feature.aggregation)
}
----

Conditionally applies aggregation when the corresponding report plugins are present in subprojects.

== Version Platform Plugin

Creates a Java Platform (BOM) with version constraints from the version catalog.

Plugin ID:: `dev.daymor.ultimanexus.jvm.gradle.feature.version-platform`

[source,kotlin]
----
plugins {
    alias(libs.plugins.ultimanexus.jvm.feature.version.platform)
}
----

=== Configuration

[source,kotlin]
----
versionPlatform {
    includeSpringBootBom = true
    springBootBomLibrary = "spring-boot-dependencies"
}
----

[source,properties]
----
versionPlatform.includeSpringBootBom=false
versionPlatform.springBootBomLibrary=spring-boot-dependencies
----

When the `org.springframework.boot` plugin is applied, the Spring Boot BOM is automatically included.

IMPORTANT: This plugin applies `java-platform` and cannot be combined with `java` or `java-library`.

== Use All Catalog Versions Plugin

Applies `java-platform` and creates strict version constraints for every entry in the version catalog.

Plugin ID:: `dev.daymor.ultimanexus.jvm.gradle.feature.use-all-catalog-versions`

[source,kotlin]
----
plugins {
    alias(libs.plugins.ultimanexus.jvm.feature.use.all.catalog.versions)
}
----

IMPORTANT: Use only in dedicated platform/BOM projects. Cannot be combined with `java` or `java-library`.

== Starter Bundle

For starter subprojects that aggregate dependencies without containing source code.

Plugin ID:: `dev.daymor.ultimanexus.jvm.gradle.bundle.starter`

[source,kotlin]
----
plugins {
    alias(libs.plugins.ultimanexus.jvm.bundle.starter)
}

dependencies {
    api(libs.spring.boot.starter.web)
    api(project(":my-common-utils"))
}
----

Includes: `java-library` + lifecycle + identity + dependency-rules + publish.

== Platform Bundle

For BOM (Bill of Materials) projects that manage dependency versions.

Plugin ID:: `dev.daymor.ultimanexus.jvm.gradle.bundle.platform`

[source,kotlin]
----
plugins {
    alias(libs.plugins.ultimanexus.jvm.bundle.platform)
}

dependencies {
    constraints {
        api(libs.guava)
        api(libs.jackson.core)
    }
}
----

Includes: `version-platform` + `identity` + `dependency-versions`.

== Root Project Bundle

For the root `build.gradle.kts` of multi-module projects.

Plugin ID:: `dev.daymor.ultimanexus.jvm.gradle.bundle.gradle-project-root`

[source,kotlin]
----
plugins {
    alias(libs.plugins.ultimanexus.jvm.bundle.gradle.project.root)
}
----

Includes: `lifecycle.root` + `dependencies.root` + `format-gradle.root` + `aggregation`.

== JVM Complete Bundle

For Kotlin, Scala, Groovy, or other JVM languages. Provides quality checks, testing, and reporting without Java-specific features.

Plugin ID:: `dev.daymor.ultimanexus.jvm.gradle.bundle.jvm-complete`

[source,kotlin]
----
plugins {
    alias(libs.plugins.ultimanexus.jvm.bundle.jvm.complete)
    kotlin("jvm")  // Add your language plugin
}
----

Includes: Gradle project base + quality checks + testing + reporting.

Does NOT include: `compile-java`, `javadoc`, `jspecify`, `lombok`, or any Spring Boot plugin.

== Ultima Nexus JVM Application Bundle

The all-in-one bundle for Ultima Nexus framework applications. **No dependency configuration needed** — the plugin automatically adds the starter, annotation module, and annotation processor.

Plugin ID:: `dev.daymor.ultimanexus.jvm.gradle.bundle.ultima-nexus-jvm-application`

[source,kotlin]
----
plugins {
    alias(libs.plugins.ultimanexus.jvm.bundle.ultima.nexus.jvm.application)
}
// That's it — no dependencies block needed
----

Includes:

* Spring Boot application with GraalVM Native Image and AOT support
* Quality checks (Checkstyle, PMD, SpotBugs, Spotless)
* Testing infrastructure (unit, integration, functional, performance)
* Lombok and JSpecify annotation processing
* Documentation (auto-detected via `antora-playbook.yml`)
* **Ultima Nexus starter** (`implementation` — the archetype's starter)
* **Ultima Nexus annotations** (`compileOnly` — `@GenerateStack`, `@GenerateApplication`, etc.)
* **Ultima Nexus processor** (`annotationProcessor` — selected from the archetype and Hibernate setting)

=== Configuration

[cols="2,1,3"]
|===
| Property | Default | Description

| `ultimaNexusJvm.archetype`
| `classic-backend`
| The application shape. Determines the starter and the processor. Supported: `classic-backend`, `classic-fullstack`, `performance-backend`, `performance-fullstack`, `reactive-backend`, `reactive-fullstack`, `base`, `entity-only`.

| `ultimaNexusJvm.useHibernate`
| `true`
| Use Hibernate generation. Set to `false` for plain JPA identity methods. Ignored for the reactive archetypes, which persist through R2DBC.
|===

=== Starter Selection

[cols="1,2"]
|===
| Archetype | Starter

| `classic-backend` / `classic-fullstack`
| `starter-spring-classic-backend` / `starter-spring-classic-fullstack`

| `performance-backend` / `performance-fullstack`
| `starter-spring-performance-backend` / `starter-spring-performance-fullstack`

| `reactive-backend` / `reactive-fullstack`
| `starter-spring-reactive-backend` / `starter-spring-reactive-fullstack`

| `base`
| `starter-spring-base` — the hexagonal core, no opinionated wiring

| `entity-only`
| none — entity generation only, no application layers
|===

=== Processor Selection

The annotation processor is selected automatically from the archetype and the Hibernate setting:

[cols="1,1,2"]
|===
| Archetype | Hibernate | Processor

| `classic-*`, `performance-*`, `base`
| `true` (default)
| `processor-java-spring-hibernate` — full hexagonal stack with Hibernate

| `classic-*`, `performance-*`, `base`
| `false`
| `processor-java-spring-application` — full hexagonal stack with plain identity

| `reactive-*`
| n/a
| `processor-java-spring-application` — full hexagonal stack; persistence is R2DBC

| `entity-only`
| `true`
| `processor-java-hibernate` — entity generation only with Hibernate

| `entity-only`
| `false`
| `processor-java` — entity generation only with plain identity
|===

=== Version Catalog Overrides

To override the auto-included dependencies, define these entries in your `libs.versions.toml`:

[source,toml]
----
[libraries]
ultima-nexus-jvm-starter = { module = "dev.daymor.ultimanexus.jvm:starter-spring-classic-backend", version = "latest.release" }
ultima-nexus-jvm-annotation = { module = "dev.daymor.ultimanexus.jvm:annotation-jvm", version = "latest.release" }
ultima-nexus-jvm-processor = { module = "dev.daymor.ultimanexus.jvm:processor-java-spring-hibernate", version = "latest.release" }
----

If these catalog entries are present, they take precedence over the fallback coordinates.

== Schema Docs Plugin

Registers an opt-in `generateSchemaDocs` task that collects the Mermaid ER and DBML schema documentation the annotation processor generates into a single output directory, ready to embed in a docs site.

Plugin ID:: `dev.daymor.ultimanexus.jvm.gradle.feature.schema-docs`

[source,kotlin]
----
plugins {
    id("dev.daymor.ultimanexus.jvm.gradle.feature.schema-docs")
}
----

== Ultima Nexus JVM Library Bundle

The most comprehensive library bundle.

Plugin ID:: `dev.daymor.ultimanexus.jvm.gradle.bundle.ultima-nexus-jvm-library`

[source,kotlin]
----
plugins {
    alias(libs.plugins.ultimanexus.jvm.bundle.ultima.nexus.jvm.library)
}
----

Includes: Spring Boot library + quality checks + testing + reporting + publishing + Lombok + JSpecify + documentation.

== Properties Reference

[cols="2,1,3"]
|===
| Property | Default | Description

| `ultimaNexusJvm.archetype`
| `classic-backend`
| Application shape: `classic-backend`, `classic-fullstack`, `performance-backend`, `performance-fullstack`, `reactive-backend`, `reactive-fullstack`, `base`, or `entity-only`

| `ultimaNexusJvm.useHibernate`
| `true`
| Use Hibernate

| `versionPlatform.includeSpringBootBom`
| `false` (auto-enabled with Spring Boot)
| Whether to include Spring Boot BOM

| `versionPlatform.springBootBomLibrary`
| `spring-boot-dependencies`
| Catalog library name for Spring Boot BOM
|===
